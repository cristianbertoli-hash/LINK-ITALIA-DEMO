
(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const progress=$('#progress');
const updateProgress=()=>{const max=document.documentElement.scrollHeight-innerHeight; if(progress) progress.style.width=(max>0?scrollY/max*100:0)+'%';}; addEventListener('scroll',updateProgress,{passive:true}); updateProgress();
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.1}); $$('.reveal').forEach(el=>obs.observe(el));
const toggle=$('#menu-toggle'), mobile=$('#mobile-menu'); if(toggle&&mobile){toggle.addEventListener('click',()=>{const o=mobile.classList.toggle('open');toggle.setAttribute('aria-expanded',String(o));}); $$('a',mobile).forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');toggle.setAttribute('aria-expanded','false')}));}
addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--mx',(e.clientX/innerWidth*100)+'%');document.documentElement.style.setProperty('--my',(e.clientY/innerHeight*100)+'%');},{passive:true});
$$('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{if(e.pointerType==='touch')return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(850px) rotateX(${-y*6}deg) rotateY(${x*7}deg) translateY(-3px)`});card.addEventListener('pointerleave',()=>card.style.transform='');card.addEventListener('pointerdown',e=>{if(e.pointerType==='touch')card.style.transform='translateY(-7px) scale(.993)'});card.addEventListener('pointerup',()=>card.style.transform='')});
// Modal system: content is pulled from hidden templates; animation expands from the clicked control.
const modalRoot=$('#modal-root'), modalPanel=$('.modal-panel',modalRoot||document), modalTitle=$('#modal-title'), modalBody=$('#modal-body'), modalKicker=$('#modal-kicker'); let lastFocus=null;
function openModal(id,trigger){const tpl=$(`#${CSS.escape(id)}`); if(!tpl||!modalRoot)return;lastFocus=trigger||document.activeElement;const r=trigger?.getBoundingClientRect();const x=r?((r.left+r.width/2)/innerWidth*100):50,y=r?((r.top+r.height/2)/innerHeight*100):50;modalPanel.style.setProperty('--modal-x',x+'%');modalPanel.style.setProperty('--modal-y',y+'%');modalTitle.textContent=tpl.dataset.title||'Approfondimento';modalKicker.textContent=tpl.dataset.kicker||'LINK-ITALIA & PARTNERS';modalBody.innerHTML=tpl.innerHTML;modalRoot.classList.add('open');modalRoot.setAttribute('aria-hidden','false');document.body.classList.add('modal-open');setTimeout(()=>$('.modal-close',modalRoot)?.focus(),40)}
function closeModal(){if(!modalRoot)return;modalRoot.classList.remove('open');modalRoot.setAttribute('aria-hidden','true');document.body.classList.remove('modal-open');setTimeout(()=>lastFocus?.focus?.(),60)}
$$('[data-modal]').forEach(btn=>btn.addEventListener('click',e=>{e.preventDefault();openModal(btn.dataset.modal,btn)})); $$('[data-modal-close]').forEach(b=>b.addEventListener('click',closeModal)); $('.modal-backdrop',modalRoot||document)?.addEventListener('click',closeModal); addEventListener('keydown',e=>{if(e.key==='Escape'&&modalRoot?.classList.contains('open'))closeModal();if(e.key==='Tab'&&modalRoot?.classList.contains('open')){const f=$$('button,a,input,textarea,select,[tabindex]:not([tabindex="-1"])',modalPanel).filter(x=>!x.disabled);if(!f.length)return;const a=f[0],z=f[f.length-1];if(e.shiftKey&&document.activeElement===a){e.preventDefault();z.focus()}else if(!e.shiftKey&&document.activeElement===z){e.preventDefault();a.focus()}}});
// Contact demo never sends data.
$$('[data-demo-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const out=$('[data-form-status]',f);if(out)out.textContent='Demo: modulo non collegato a un server. In produzione invierebbe la richiesta in modo sicuro.';}));
// Canvas network with a brief shockwave on impact.
const c=$('#network'); if(c){const ctx=c.getContext('2d');let w=0,h=0,dpr=1,nodes=[],shock=0,shockX=65,shockY=38;
function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const count=w<700?24:52;nodes=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.14,vy:(Math.random()-.5)*.14,r:Math.random()*1.15+.4}))}
function draw(){ctx.clearRect(0,0,w,h);for(let i=0;i<nodes.length;i++){const a=nodes[i];a.x+=a.vx;a.y+=a.vy;if(a.x<-30)a.x=w+30;if(a.x>w+30)a.x=-30;if(a.y<-30)a.y=h+30;if(a.y>h+30)a.y=-30;if(shock>0){const dx=a.x-shockX,dy=a.y-shockY,d=Math.hypot(dx,dy);if(d<190){const p=(1-d/190)*shock*.006;a.x+=dx*p;a.y+=dy*p}}ctx.beginPath();ctx.arc(a.x,a.y,a.r,0,Math.PI*2);ctx.fillStyle='rgba(255,255,255,.28)';ctx.fill();for(let j=i+1;j<nodes.length;j++){const b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<145){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(180,42,75,${(1-d/145)*.16})`;ctx.lineWidth=.7;ctx.stroke()}}}if(shock>0)shock*=.90;requestAnimationFrame(draw)}
addEventListener('resize',resize,{passive:true});resize();draw();
window.__linkImpact=()=>{shock=100;shockX=65;shockY=38;};}
// Recurring abstract impact: a red impulse travels from right to the target in the logo.
const projectile=$('#impact-projectile'),anchor=$('#impact-core');function impact(){if(!projectile||!anchor)return;const r=anchor.getBoundingClientRect(),w=projectile.getBoundingClientRect().width||42,centerX=r.left+r.width/2,centerY=r.top+r.height/2,left=(window.LinkCmsCore?.projectileLeft?window.LinkCmsCore.projectileLeft(centerX,w):centerX-w);projectile.style.setProperty('--impact-left',left+'px');projectile.style.top=(centerY-1.5)+'px';projectile.classList.remove('fly');void projectile.offsetWidth;projectile.classList.add('fly');setTimeout(()=>{anchor.classList.remove('hit');void anchor.offsetWidth;anchor.classList.add('hit');window.__linkImpact?.();},800);setTimeout(()=>anchor.classList.remove('hit'),1800)}setTimeout(impact,900);setInterval(impact,7200);
})();
